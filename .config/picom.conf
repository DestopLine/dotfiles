vsync = true;
backend = "glx";
glx-no-stencil = true;
unredir-if-possible = true;

shadow = true;
shadow-radius = 15;
shadow-offset-x = -10;
shadow-offset-y = -10;

# blur-method = "dual_kawase";
# blur-background-exclude = [
	# "window_type != 'dock'"
# ];
